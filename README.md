# Averone Minimal SPA

A minimal Vite + React + TypeScript project with Tailwind CSS.

## Setup

1. `npm install`
2. `npm run dev`

## Scripts

- `npm run dev` - start local dev server
- `npm run build` - build production assets
- `npm run preview` - preview production build
